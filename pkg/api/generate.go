package api

//go:generate sh -c "mkdir -p web_dist && test -f web_dist/index.html || echo '<!DOCTYPE html><html><body><p>Run make build to include the web UI</p></body></html>' > web_dist/index.html"
